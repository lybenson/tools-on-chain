// 'use client'
// import { Button } from 'antd'
// export default Button

/**
 * Import this component into either the client-side or server-side component. When imported into a server-side component, the rendering of this component will occur on the client-side.
 *
 */

export {}
